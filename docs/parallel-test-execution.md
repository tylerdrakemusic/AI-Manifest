# Parallel test execution

The canonical command is `python tools/run_tests.py`. It loads `tools/parallel_test_policy.json`, composes its excluded markers with any `pytest.ini` `addopts` marker expression, emits concise output, and returns pytest's nonzero status on any worker failure. Serial and `--parallel` modes use the same exclusions; `--parallel` is only a reviewed local evidence run and requires `pytest-xdist`.

The serial run was attempted from branch head `a712052b45cdadb6d5b3b2ee147b74e5b9454619` on 2026-08-29 UTC with `python tools/run_tests.py --junitxml tmp/serial-baseline.xml`. At 18:04:53Z to 18:04:54Z, pytest collected 267 tests but stopped during collection with 1 error and exit code 2. The error was the missing `WORKSPACE_ROOT` or `WORKSPACE_BACKUP_ENGINE_PATH` required by the shared database-backup contract. JUnit records 1 error and no completed test result set. This is not a comparable baseline and is not evidence that parallel execution is safe. CI remains serial and pending a clean contract-complete rerun. Existing `playwright` and `live` exclusions are unchanged. `FR-20260826-workspace-ci-exclusion-audit` is a rebase/conflict dependency for changing the exclusion policy.

The contract-complete collection was run from branch head `a712052b45cdadb6d5b3b2ee147b74e5b9454619` on 2026-08-29 UTC with `C:\G\python.exe -m pytest --collect-only -q -o addopts= -m "not playwright and not live"`, `WORKSPACE_ROOT=F:\⊕Workspace`, `WORKSPACE_BACKUP_ENGINE_PATH=F:\⊕Workspace\src\utils\database_backup.py`, and `PLAYWRIGHT_ENABLED=0`. Collection completed with 283 tests collected and 26 deselected, exit code 0. This validates the documented shared database-backup contract only; it is not a completed serial baseline and is not evidence that parallel execution is safe. The runner does not accept pytest passthrough flags, so the earlier `C:\G\python.exe tools/run_tests.py --collect-only -q -o addopts= -m "not playwright and not live"` attempt exited 2 with `unrecognized arguments`; that command must not be used for collection. CI remains serial and no parallel-safety claim is approved. Existing `playwright` and `live` exclusions are unchanged. `FR-20260826-workspace-ci-exclusion-audit` is a rebase/conflict dependency for changing the exclusion policy.

Serial fallback: `C:\G\python.exe tools/run_tests.py --junitxml tmp/serial.xml`.