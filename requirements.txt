# 👁AI-Manifest — Python dependencies
# Core
elevenlabs>=1.0
httpx>=0.27
pydantic>=2.0
flask>=3.0

# Audio
sounddevice>=0.4
soundfile>=0.12

# Alexa integration
ask-sdk>=1.19
flask-ask-sdk>=1.0
oscrypto>=1.3.0  # pin for OpenSSL 3.x compat on Ubuntu 22.04+ CI runners
waitress>=3.0

# Testing (dev)
pytest>=8.0
pytest-cov>=5.0
pytest-mock>=3.14

# Model Context Protocol servers
mcp>=1.1.0
